/**
 * Handle malformed input
 */
export function handleTypeError(_bud, label, { error }) {
    const x = `\u2717`;
    const err = new Error(error.issues
        .map(issue => `${x} ${label} -> ${issue.path}: ${issue.message}`)
        .join(`\n\n`)
        .concat(`\n`));
    err.name = `Config error`;
    throw err;
}
//# sourceMappingURL=handleValidationTypeError.js.map