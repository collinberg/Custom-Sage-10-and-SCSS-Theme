import type { Bud } from '@roots/bud-framework/bud';
import type { SyncCallback, SyncRegistry, SyncStore } from '@roots/bud-framework/registry';
import { Hooks } from '../base/base.js';
/**
 * Synchronous hooks registry
 */
export declare class SyncHooks extends Hooks<SyncStore> {
    /**
     * Set a value
     *
     * @public
     * @decorator `@bind`
     */
    set<T extends `${keyof SyncStore & string}`>(id: T, ...input: Array<SyncCallback[T]>): Bud;
    /**
     * Set multiple values
     *
     * @public
     * @decorator `@bind`
     */
    setRecords(map: Partial<SyncCallback>): Bud;
    /**
     * Get a value
     *
     * @public
     * @decorator `@bind`
     */
    get<T extends `${keyof SyncRegistry & string}`>(id: T, fallback?: SyncCallback[T]): any;
}
//# sourceMappingURL=sync.d.ts.map