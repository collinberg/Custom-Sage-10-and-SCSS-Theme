import React from '@roots/bud-support/react';
import { Message } from './message.js';
export const Stderr = ({ message }) => (React.createElement(Message, { label: "stderr", color: "red", message: message }));
export const Stdout = ({ message }) => (React.createElement(Message, { label: "stdout", color: "green", message: message }));
export const Log = ({ stream, message }) => {
    switch (stream) {
        case `stdout`:
            return React.createElement(Stdout, { message: message });
        case `stderr`:
            return React.createElement(Stderr, { message: message });
    }
};
//# sourceMappingURL=log.js.map