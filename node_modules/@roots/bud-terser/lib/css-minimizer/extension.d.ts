import type { Bud } from '@roots/bud-framework';
import { Extension } from '@roots/bud-framework/extension';
import type { BasePluginOptions, Plugin } from '@roots/bud-support/css-minimizer-webpack-plugin';
/**
 * Terser extension
 *
 * @public
 * @decorator `@label`
 * @decorator `@expose`
 * @decorator `@options`
 * @decorator `@disabled`
 */
export declare class BudMinimizeCss extends Extension<BasePluginOptions, Plugin> {
    /**
     * `buildBefore` callback
     *
     * @public
     * @decorator `@bind`
     */
    buildBefore({ hooks, module }: Bud): Promise<void>;
}
//# sourceMappingURL=extension.d.ts.map