import isUndefined from '@roots/bud-support/lodash/isUndefined';
export const isLockConflict = (bud) => !isUndefined(bud.context.config?.[`yarn.lock`]) &&
    !isUndefined(bud.context.config?.[`package-lock.json`]);
export const isNoLock = (bud) => !bud.context.config?.[`yarn.lock`] &&
    !bud.context.config?.[`package-lock.json`] &&
    !bud.context.config?.[`package.json`]?.module?.packageManager;
export const lockError = (bud) => isLockConflict(bud) || isNoLock(bud);
//# sourceMappingURL=isLockfileError.js.map