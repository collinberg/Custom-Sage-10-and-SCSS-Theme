import type { Bud } from '@roots/bud-framework';
import { Extension } from '@roots/bud-framework/extension';
import type { Options, Plugin } from '@roots/bud-support/html-webpack-plugin';
export type { Options };
/**
 * `@roots/bud-extensions/html-webpack-plugin` adapter
 *
 * @public
 * @decorator `@label`
 * @decorator `@plugin`
 */
export default class BudHtmlWebpackPlugin extends Extension<Options, Plugin> {
    /**
     * Make extension
     *
     * @param bud - bud instance
     * @param options - plugin options
     *
     * @public
     */
    make(bud: Bud, options: Options): Promise<any>;
}
//# sourceMappingURL=index.d.ts.map