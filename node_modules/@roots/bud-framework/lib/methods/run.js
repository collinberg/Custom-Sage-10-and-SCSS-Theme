export const run = async function () {
    if (this.isProduction) {
        return await this.compiler.compile().then(compilation => compilation?.run((error, stats) => {
            if (error)
                throw error.message;
            if (this.isProduction)
                compilation.close(error => {
                    if (error)
                        throw error.message;
                });
        }));
    }
    await this.server.run();
};
//# sourceMappingURL=run.js.map