import type { Bud } from '@roots/bud-framework';
import { Extension } from '@roots/bud-framework/extension';
import type Webpack from '@roots/bud-support/webpack';
/**
 * This is the extension that enables hot module replacement in `development` mode
 *
 * @public
 * @decorator `@label`
 * @decorator `@plugin`
 * @decorator `@development`
 */
export default class BudHMR extends Extension<{}, Webpack.HotModuleReplacementPlugin> {
    make(bud: Bud): Promise<any>;
    when(bud: Bud): Promise<boolean>;
}
//# sourceMappingURL=index.d.ts.map