export const when = (when) => (constructor) => class extends constructor {
    constructor(...args) {
        super(...args);
        this.when = when.bind ? when.bind(this) : when;
    }
};
//# sourceMappingURL=when.js.map