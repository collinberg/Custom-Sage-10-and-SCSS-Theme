import { Bud, Extension } from '@roots/bud-framework';
import Webpack from '@roots/bud-support/webpack';
/**
 * `@roots/bud-extensions/webpack-define-plugin` adapter
 *
 * @public
 * @decorator `@label`
 * @decorator `@plugin`
 */
export default class BudDefine extends Extension<Webpack.DefinePlugin['definitions'], Webpack.DefinePlugin> {
    /**
     * `init` callback
     *
     * @public
     * @decorator `@bind`
     */
    init({ env }: Bud): Promise<void>;
    /**
     * `when` callback
     *
     * @public
     * @decorator `@bind`
     */
    when(_bud: Bud, options?: Webpack.DefinePlugin['definitions']): Promise<boolean>;
}
//# sourceMappingURL=index.d.ts.map