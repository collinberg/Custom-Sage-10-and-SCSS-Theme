import type { Bud } from '@roots/bud-framework';
import type { EventsCallback, EventsStore } from '@roots/bud-framework/registry';
import { Hooks } from '../base/base.js';
/**
 * Synchronous hooks registry
 *
 * @remarks
 * Supports sync values
 */
export declare class EventHooks extends Hooks<EventsStore> {
    /**
     * Register a function to filter a value.
     *
     * @public
     * @decorator `@bind`
     */
    set<T extends keyof EventsStore & string>(id: T, ...input: Array<EventsCallback>): Bud;
    /**
     * Fire actions registered to an event.
     *
     * @example
     * ```js
     * await app.hooks.fire('namespace.key')
     * ```
     *
     * @public
     * @decorator `@bind`
     */
    get<T extends keyof EventsStore & string>(id: T): Promise<Bud>;
}
//# sourceMappingURL=event.d.ts.map