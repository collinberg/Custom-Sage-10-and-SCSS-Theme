import Ink from '@roots/bud-support/ink';
import React from '@roots/bud-support/react';
import Compilation from './compilation/compilation.component.js';
import { Server } from './server/index.js';
const App = ({ compilations, context, devUrl, proxyUrl, displayServerInfo, displayAssets, displayEntrypoints, publicDevUrl, publicProxyUrl, mode, watchFiles = new Set(), }) => {
    if (!compilations.length)
        return (React.createElement(Ink.Box, { flexDirection: "column" },
            React.createElement(Ink.Text, null, "No compilations")));
    return (React.createElement(Ink.Box, { flexDirection: "column" },
        compilations.map((compilation, id) => (React.createElement(Ink.Box, { key: id, flexDirection: "column", paddingY: 1 },
            React.createElement(Compilation, { compilation: compilation, context: context, displayAssets: displayAssets, displayEntrypoints: displayEntrypoints })))),
        mode === `development` ? (React.createElement(Server, { devUrl: devUrl, publicDevUrl: publicDevUrl, proxyUrl: proxyUrl, publicProxyUrl: publicProxyUrl, watchFiles: watchFiles, displayServerInfo: displayServerInfo })) : null));
};
export default App;
//# sourceMappingURL=app.js.map