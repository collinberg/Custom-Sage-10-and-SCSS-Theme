import { Bud, Extension } from '@roots/bud-framework';
import type { Options } from '@roots/bud-support/webpack-manifest-plugin';
import { Plugin } from '@roots/bud-support/webpack-manifest-plugin';
/**
 * `webpack-manifest-plugin` adapter
 *
 * @public
 * @decorator `@label`
 * @decorator `@plugin`
 * @decorator `@options`
 */
export default class BudManifestExtension extends Extension<Options, Plugin> {
    /**
     * `when` callback
     *
     * @remarks
     * Returns `feature.manifest` hook result
     *
     * @public
     * @decorator `@bind`
     */
    when(bud: Bud): Promise<boolean>;
}
//# sourceMappingURL=index.d.ts.map