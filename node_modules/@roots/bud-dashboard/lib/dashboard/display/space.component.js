import Ink from '@roots/bud-support/ink';
import React from '@roots/bud-support/react';
import { VERT } from '../format.js';
const Space = ({ children, final = false, }) => {
    const arrayedChildren = Array.isArray(children) ? children : [children];
    return (React.createElement(Ink.Box, { flexDirection: "column" }, arrayedChildren.map((Child, index) => (React.createElement(Ink.Box, { key: index, flexDirection: "row" },
        React.createElement(Ink.Text, { dimColor: true }, !final ? VERT : `  `),
        Child)))));
};
export default Space;
//# sourceMappingURL=space.component.js.map