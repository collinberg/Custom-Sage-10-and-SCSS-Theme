export const isYarn = (bud) => bud.context.config?.[`yarn.lock`] ||
    bud.context.manifest?.packageManager?.includes(`yarn`);
export const isNpm = (bud) => bud.context.config?.[`package-lock.json`] ||
    bud.context.manifest?.packageManager?.includes(`npm`);
export const detectPackageManager = (bud) => isYarn(bud) ? `yarn` : isNpm(bud) ? `npm` : false;
//# sourceMappingURL=detectPackageManager.js.map