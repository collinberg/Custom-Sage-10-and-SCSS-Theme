import { Extension } from '@roots/bud-framework';
import Container from '@roots/container';
import ThemeJsonWebpackPlugin, { Options, Theme } from '@roots/wordpress-theme-json-webpack-plugin';
/**
 * Callback function used to configure wordpress `theme.json`
 */
export interface Mutator {
    (json: Partial<Theme.GlobalSettingsAndStyles['settings']> | Container<Partial<Theme.GlobalSettingsAndStyles['settings']>>): Partial<Theme.GlobalSettingsAndStyles['settings']> | Container<Partial<Theme.GlobalSettingsAndStyles['settings']>>;
}
/**
 * WP Theme JSON plugin adapter
 *
 * @remarks
 * Produces a WordPress `theme.json`
 *
 * @example
 * ```ts
 * bud.themeJson(theme =>
 *   theme.set('color', {})
 * )
 * ```
 *
 * @public
 * @decorator `@label`
 * @decorator `@options`
 * @decorator `@when`
 * @decorator `@plugin`
 * @decorator `@expose`
 * @decorator `@disabled`
 */
export declare class WordPressThemeJSON extends Extension<Options, ThemeJsonWebpackPlugin> {
    settings(input?: Mutator | Container<Partial<Theme.GlobalSettingsAndStyles['settings']>> | Partial<Theme.GlobalSettingsAndStyles['settings']> | boolean, raw?: boolean): this;
}
//# sourceMappingURL=extension.d.ts.map