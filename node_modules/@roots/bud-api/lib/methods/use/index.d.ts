import type { Bud, Modules } from '@roots/bud-framework';
import type { Constructor, Extension, ExtensionLiteral } from '@roots/bud-framework/extension';
export type Parameters = [
    Extension | Constructor | ExtensionLiteral | `${keyof Modules & string}` | Array<Extension | Constructor | ExtensionLiteral | `${keyof Modules & string}`>
];
export interface use {
    (...source: Parameters): Promise<Bud>;
}
export declare const use: use;
//# sourceMappingURL=index.d.ts.map