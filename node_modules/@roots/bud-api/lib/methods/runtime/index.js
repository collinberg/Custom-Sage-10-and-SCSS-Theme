/**
 * Default options for runtime if no options are passed as parameters.
 */
const DEFAULT_RUNTIME = {
    name: (entrypoint) => `runtime/${entrypoint.name}`,
};
export const runtime = async function (runtime = DEFAULT_RUNTIME) {
    return this.hooks.on(`build.optimization.runtimeChunk`, runtime === true ? DEFAULT_RUNTIME : runtime);
};
//# sourceMappingURL=index.js.map