/**
 * Extension to be run in `development` mode
 *
 * @remarks
 * Extension decorator
 */
export const development = (constructor) => class extends constructor {
    constructor(...args) {
        super(...args);
    }
    /**
     * `when` callback
     *
     * @returns `true` when mode is `development`
     * @public
     */
    when(bud) {
        return bud.isDevelopment;
    }
};
//# sourceMappingURL=development.js.map