import Ink from '@roots/bud-support/ink';
import React from '@roots/bud-support/react';
import Title from '../display/title.component.js';
import { color, size as formatSize } from '../format.js';
const Asset = ({ minWidth, name, size, emitted, final, cached, indent, info, }) => {
    return (React.createElement(Title, { indent: indent, final: final },
        React.createElement(Ink.Box, { minWidth: minWidth, marginRight: 1 },
            React.createElement(Ink.Text, { color: color.dim }, name)),
        size && size > 0 ? (React.createElement(Ink.Box, { minWidth: 10, justifyContent: "flex-end" },
            React.createElement(Ink.Text, { color: color.dim, dimColor: true }, formatSize(size).trim()))) : null));
};
export default Asset;
//# sourceMappingURL=asset.component.js.map