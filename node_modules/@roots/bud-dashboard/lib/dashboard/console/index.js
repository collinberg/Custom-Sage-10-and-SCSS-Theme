import Ink from '@roots/bud-support/ink';
import React from '@roots/bud-support/react';
import { Log, Stderr, Stdout } from './log.js';
export const Console = ({ messages }) => {
    if (!messages?.length)
        return null;
    return (React.createElement(Ink.Box, null, messages.map((item, i) => (React.createElement(Log, { key: i, ...item })))));
};
export { Log, Stdout, Stderr };
//# sourceMappingURL=index.js.map